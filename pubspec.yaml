name: cubit_impl_annotation
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none'
homepage:

environment:
  sdk: ^3.3.4

dependencies:
  failures:
    git: https://github.com/mahmood1996/failures.git
  equatable: ^2.0.5
  fpdart: any

dev_dependencies:
  test: any
